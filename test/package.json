{
  "name": "finally-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright E2E specs for FinAlly (planning/E2E_PLAN.md). Not part of the production image.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.48.0"
  }
}
